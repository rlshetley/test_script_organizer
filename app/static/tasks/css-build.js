/**
 * Gulp build task to copy and minify the application CSS generated by the LESS compiler
 */

module.exports = function (gulp, plugins, config) {
    return function () {
        return gulp.src(config.cssFiles)
          .pipe(plugins.minifyCss())
          .pipe(gulp.dest(config.cssDirectory));
    };
};